{
  "name": "mfront-doc",
  "version": "0.0.1",
  "scripts": {
    "start": "pnpm --filter starter run start",
    "build": "pnpm --filter starter run build"
  },
  "devDependencies": {
    "@types/node": "^25.3.2"
  }
}